const injectData = require('./lib/inject-data')
const expandComponents = require('./lib/expand-components')
const renderTemplate = require('./lib/render-template')

module.exports = {
  injectData,
  expandComponents,
  renderTemplate
}
